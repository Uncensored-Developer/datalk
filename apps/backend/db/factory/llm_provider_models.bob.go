// Code generated by BobGen psql v0.44.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package factory

import (
	"context"
	"testing"
	"time"

	models "github.com/Uncensored-Developer/datalk/apps/backend/db/models"
	"github.com/aarondl/opt/null"
	"github.com/aarondl/opt/omit"
	"github.com/aarondl/opt/omitnull"
	"github.com/jaswdr/faker/v2"
	"github.com/stephenafamo/bob"
)

type LLMProviderModelMod interface {
	Apply(context.Context, *LLMProviderModelTemplate)
}

type LLMProviderModelModFunc func(context.Context, *LLMProviderModelTemplate)

func (f LLMProviderModelModFunc) Apply(ctx context.Context, n *LLMProviderModelTemplate) {
	f(ctx, n)
}

type LLMProviderModelModSlice []LLMProviderModelMod

func (mods LLMProviderModelModSlice) Apply(ctx context.Context, n *LLMProviderModelTemplate) {
	for _, f := range mods {
		f.Apply(ctx, n)
	}
}

// LLMProviderModelTemplate is an object representing the database table.
// all columns are optional and should be set by mods
type LLMProviderModelTemplate struct {
	ID               func() int64
	ProviderConfigID func() int64
	Model            func() string
	DisplayName      func() string
	ContextWindow    func() null.Val[int32]
	SupportsSQL      func() bool
	IsEnabled        func() bool
	DiscoveredAt     func() time.Time

	r llmProviderModelR
	f *Factory

	alreadyPersisted bool
}

type llmProviderModelR struct {
	ProviderConfigLLMProviderConfig *llmProviderModelRProviderConfigLLMProviderConfigR
}

type llmProviderModelRProviderConfigLLMProviderConfigR struct {
	o *LLMProviderConfigTemplate
}

// Apply mods to the LLMProviderModelTemplate
func (o *LLMProviderModelTemplate) Apply(ctx context.Context, mods ...LLMProviderModelMod) {
	for _, mod := range mods {
		mod.Apply(ctx, o)
	}
}

// setModelRels creates and sets the relationships on *models.LLMProviderModel
// according to the relationships in the template. Nothing is inserted into the db
func (t LLMProviderModelTemplate) setModelRels(o *models.LLMProviderModel) {
	if t.r.ProviderConfigLLMProviderConfig != nil {
		rel := t.r.ProviderConfigLLMProviderConfig.o.Build()
		o.ProviderConfigID = rel.ID // h2
		o.R.ProviderConfigLLMProviderConfig = rel
		o.R.Loaded.ProviderConfigLLMProviderConfig = true
	}
}

// BuildSetter returns an *models.LLMProviderModelSetter
// this does nothing with the relationship templates
func (o LLMProviderModelTemplate) BuildSetter() *models.LLMProviderModelSetter {
	m := &models.LLMProviderModelSetter{}

	if o.ID != nil {
		val := o.ID()
		m.ID = omit.From(val)
	}
	if o.ProviderConfigID != nil {
		val := o.ProviderConfigID()
		m.ProviderConfigID = omit.From(val)
	}
	if o.Model != nil {
		val := o.Model()
		m.Model = omit.From(val)
	}
	if o.DisplayName != nil {
		val := o.DisplayName()
		m.DisplayName = omit.From(val)
	}
	if o.ContextWindow != nil {
		val := o.ContextWindow()
		m.ContextWindow = omitnull.FromNull(val)
	}
	if o.SupportsSQL != nil {
		val := o.SupportsSQL()
		m.SupportsSQL = omit.From(val)
	}
	if o.IsEnabled != nil {
		val := o.IsEnabled()
		m.IsEnabled = omit.From(val)
	}
	if o.DiscoveredAt != nil {
		val := o.DiscoveredAt()
		m.DiscoveredAt = omit.From(val)
	}

	return m
}

// BuildManySetter returns an []*models.LLMProviderModelSetter
// this does nothing with the relationship templates
func (o LLMProviderModelTemplate) BuildManySetter(number int) []*models.LLMProviderModelSetter {
	m := make([]*models.LLMProviderModelSetter, number)

	for i := range m {
		m[i] = o.BuildSetter()
	}

	return m
}

// Build returns an *models.LLMProviderModel
// Related objects are also created and placed in the .R field
// NOTE: Objects are not inserted into the database. Use LLMProviderModelTemplate.Create
func (o LLMProviderModelTemplate) Build() *models.LLMProviderModel {
	m := &models.LLMProviderModel{}

	if o.ID != nil {
		m.ID = o.ID()
	}
	if o.ProviderConfigID != nil {
		m.ProviderConfigID = o.ProviderConfigID()
	}
	if o.Model != nil {
		m.Model = o.Model()
	}
	if o.DisplayName != nil {
		m.DisplayName = o.DisplayName()
	}
	if o.ContextWindow != nil {
		m.ContextWindow = o.ContextWindow()
	}
	if o.SupportsSQL != nil {
		m.SupportsSQL = o.SupportsSQL()
	}
	if o.IsEnabled != nil {
		m.IsEnabled = o.IsEnabled()
	}
	if o.DiscoveredAt != nil {
		m.DiscoveredAt = o.DiscoveredAt()
	}

	o.setModelRels(m)

	return m
}

// BuildMany returns an models.LLMProviderModelSlice
// Related objects are also created and placed in the .R field
// NOTE: Objects are not inserted into the database. Use LLMProviderModelTemplate.CreateMany
func (o LLMProviderModelTemplate) BuildMany(number int) models.LLMProviderModelSlice {
	m := make(models.LLMProviderModelSlice, number)

	for i := range m {
		m[i] = o.Build()
	}

	return m
}

func ensureCreatableLLMProviderModel(m *models.LLMProviderModelSetter) {
	if !(m.ProviderConfigID.IsValue()) {
		val := random_int64(nil)
		m.ProviderConfigID = omit.From(val)
	}
	if !(m.Model.IsValue()) {
		val := random_string(nil)
		m.Model = omit.From(val)
	}
	if !(m.DisplayName.IsValue()) {
		val := random_string(nil)
		m.DisplayName = omit.From(val)
	}
}

// insertOptRels creates and inserts any optional the relationships on *models.LLMProviderModel
// according to the relationships in the template.
// any required relationship should have already exist on the model
func (o *LLMProviderModelTemplate) insertOptRels(ctx context.Context, exec bob.Executor, m *models.LLMProviderModel) error {
	var err error

	return err
}

// Create builds a llmProviderModel and inserts it into the database
// Relations objects are also inserted and placed in the .R field
func (o *LLMProviderModelTemplate) Create(ctx context.Context, exec bob.Executor) (*models.LLMProviderModel, error) {
	var err error
	opt := o.BuildSetter()
	ensureCreatableLLMProviderModel(opt)

	// Retrieve ancestor models from context to avoid duplicate parent creation.
	// Parents are keyed by "parent_table:child_table:child_rel_name".
	// This works regardless of NoBackReferencing since it only uses child-side metadata.
	mInCreation, _ := modelsInCreationCtx.Value(ctx)

	var rel0 *models.LLMProviderConfig

	if o.r.ProviderConfigLLMProviderConfig == nil {
		if parentModel, found := mInCreation["llm_provider_configs:llm_provider_models:llm_provider_models.llm_provider_models_provider_config_id_fkey"]; found {
			if pModel, ok := parentModel.(*models.LLMProviderConfig); ok {
				rel0 = pModel
			}
		}
	}

	if rel0 == nil {
		if o.r.ProviderConfigLLMProviderConfig == nil {
			LLMProviderModelMods.WithNewProviderConfigLLMProviderConfig().Apply(ctx, o)
		}

		if o.r.ProviderConfigLLMProviderConfig.o.alreadyPersisted {
			rel0 = o.r.ProviderConfigLLMProviderConfig.o.Build()
		} else {
			rel0, err = o.r.ProviderConfigLLMProviderConfig.o.Create(ctx, exec)
			if err != nil {
				return nil, err
			}
		}
	}

	opt.ProviderConfigID = omit.From(rel0.ID)

	m, err := models.LLMProviderModels.Insert(opt).One(ctx, exec)
	if err != nil {
		return nil, err
	}

	// Store this model in context for child creates.
	// Key format: "parent_table:child_table:child_rel_name" where child_rel_name is the FK name.
	// We store an entry for every relationship pointing TO this table (where this table is the parent).
	// This works regardless of NoBackReferencing since keys use child-side metadata.
	newMInCreation := make(map[string]any, len(mInCreation)+1)
	for k, v := range mInCreation {
		newMInCreation[k] = v
	}

	ctx = modelsInCreationCtx.WithValue(ctx, newMInCreation)

	m.R.ProviderConfigLLMProviderConfig = rel0
	m.R.Loaded.ProviderConfigLLMProviderConfig = true

	if err := o.insertOptRels(ctx, exec, m); err != nil {
		return nil, err
	}
	return m, err
}

// MustCreate builds a llmProviderModel and inserts it into the database
// Relations objects are also inserted and placed in the .R field
// panics if an error occurs
func (o *LLMProviderModelTemplate) MustCreate(ctx context.Context, exec bob.Executor) *models.LLMProviderModel {
	m, err := o.Create(ctx, exec)
	if err != nil {
		panic(err)
	}
	return m
}

// CreateOrFail builds a llmProviderModel and inserts it into the database
// Relations objects are also inserted and placed in the .R field
// It calls `tb.Fatal(err)` on the test/benchmark if an error occurs
func (o *LLMProviderModelTemplate) CreateOrFail(ctx context.Context, tb testing.TB, exec bob.Executor) *models.LLMProviderModel {
	tb.Helper()
	m, err := o.Create(ctx, exec)
	if err != nil {
		tb.Fatal(err)
		return nil
	}
	return m
}

// CreateMany builds multiple llmProviderModels and inserts them into the database
// Relations objects are also inserted and placed in the .R field
func (o LLMProviderModelTemplate) CreateMany(ctx context.Context, exec bob.Executor, number int) (models.LLMProviderModelSlice, error) {
	var err error
	m := make(models.LLMProviderModelSlice, number)

	for i := range m {
		m[i], err = o.Create(ctx, exec)
		if err != nil {
			return nil, err
		}
	}

	return m, nil
}

// MustCreateMany builds multiple llmProviderModels and inserts them into the database
// Relations objects are also inserted and placed in the .R field
// panics if an error occurs
func (o LLMProviderModelTemplate) MustCreateMany(ctx context.Context, exec bob.Executor, number int) models.LLMProviderModelSlice {
	m, err := o.CreateMany(ctx, exec, number)
	if err != nil {
		panic(err)
	}
	return m
}

// CreateManyOrFail builds multiple llmProviderModels and inserts them into the database
// Relations objects are also inserted and placed in the .R field
// It calls `tb.Fatal(err)` on the test/benchmark if an error occurs
func (o LLMProviderModelTemplate) CreateManyOrFail(ctx context.Context, tb testing.TB, exec bob.Executor, number int) models.LLMProviderModelSlice {
	tb.Helper()
	m, err := o.CreateMany(ctx, exec, number)
	if err != nil {
		tb.Fatal(err)
		return nil
	}
	return m
}

// LLMProviderModel has methods that act as mods for the LLMProviderModelTemplate
var LLMProviderModelMods llmProviderModelMods

type llmProviderModelMods struct{}

func (m llmProviderModelMods) RandomizeAllColumns(f *faker.Faker) LLMProviderModelMod {
	return LLMProviderModelModSlice{
		LLMProviderModelMods.RandomID(f),
		LLMProviderModelMods.RandomProviderConfigID(f),
		LLMProviderModelMods.RandomModel(f),
		LLMProviderModelMods.RandomDisplayName(f),
		LLMProviderModelMods.RandomContextWindow(f),
		LLMProviderModelMods.RandomSupportsSQL(f),
		LLMProviderModelMods.RandomIsEnabled(f),
		LLMProviderModelMods.RandomDiscoveredAt(f),
	}
}

// Set the model columns to this value
func (m llmProviderModelMods) ID(val int64) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.ID = func() int64 { return val }
	})
}

// Set the Column from the function
func (m llmProviderModelMods) IDFunc(f func() int64) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.ID = f
	})
}

// Clear any values for the column
func (m llmProviderModelMods) UnsetID() LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.ID = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m llmProviderModelMods) RandomID(f *faker.Faker) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.ID = func() int64 {
			return random_int64(f)
		}
	})
}

// Set the model columns to this value
func (m llmProviderModelMods) ProviderConfigID(val int64) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.ProviderConfigID = func() int64 { return val }
	})
}

// Set the Column from the function
func (m llmProviderModelMods) ProviderConfigIDFunc(f func() int64) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.ProviderConfigID = f
	})
}

// Clear any values for the column
func (m llmProviderModelMods) UnsetProviderConfigID() LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.ProviderConfigID = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m llmProviderModelMods) RandomProviderConfigID(f *faker.Faker) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.ProviderConfigID = func() int64 {
			return random_int64(f)
		}
	})
}

// Set the model columns to this value
func (m llmProviderModelMods) Model(val string) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.Model = func() string { return val }
	})
}

// Set the Column from the function
func (m llmProviderModelMods) ModelFunc(f func() string) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.Model = f
	})
}

// Clear any values for the column
func (m llmProviderModelMods) UnsetModel() LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.Model = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m llmProviderModelMods) RandomModel(f *faker.Faker) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.Model = func() string {
			return random_string(f)
		}
	})
}

// Set the model columns to this value
func (m llmProviderModelMods) DisplayName(val string) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.DisplayName = func() string { return val }
	})
}

// Set the Column from the function
func (m llmProviderModelMods) DisplayNameFunc(f func() string) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.DisplayName = f
	})
}

// Clear any values for the column
func (m llmProviderModelMods) UnsetDisplayName() LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.DisplayName = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m llmProviderModelMods) RandomDisplayName(f *faker.Faker) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.DisplayName = func() string {
			return random_string(f)
		}
	})
}

// Set the model columns to this value
func (m llmProviderModelMods) ContextWindow(val null.Val[int32]) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.ContextWindow = func() null.Val[int32] { return val }
	})
}

// Set the Column from the function
func (m llmProviderModelMods) ContextWindowFunc(f func() null.Val[int32]) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.ContextWindow = f
	})
}

// Clear any values for the column
func (m llmProviderModelMods) UnsetContextWindow() LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.ContextWindow = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
// The generated value is sometimes null
func (m llmProviderModelMods) RandomContextWindow(f *faker.Faker) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.ContextWindow = func() null.Val[int32] {
			if f == nil {
				f = &defaultFaker
			}

			val := random_int32(f)
			return null.From(val)
		}
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
// The generated value is never null
func (m llmProviderModelMods) RandomContextWindowNotNull(f *faker.Faker) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.ContextWindow = func() null.Val[int32] {
			if f == nil {
				f = &defaultFaker
			}

			val := random_int32(f)
			return null.From(val)
		}
	})
}

// Set the model columns to this value
func (m llmProviderModelMods) SupportsSQL(val bool) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.SupportsSQL = func() bool { return val }
	})
}

// Set the Column from the function
func (m llmProviderModelMods) SupportsSQLFunc(f func() bool) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.SupportsSQL = f
	})
}

// Clear any values for the column
func (m llmProviderModelMods) UnsetSupportsSQL() LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.SupportsSQL = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m llmProviderModelMods) RandomSupportsSQL(f *faker.Faker) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.SupportsSQL = func() bool {
			return random_bool(f)
		}
	})
}

// Set the model columns to this value
func (m llmProviderModelMods) IsEnabled(val bool) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.IsEnabled = func() bool { return val }
	})
}

// Set the Column from the function
func (m llmProviderModelMods) IsEnabledFunc(f func() bool) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.IsEnabled = f
	})
}

// Clear any values for the column
func (m llmProviderModelMods) UnsetIsEnabled() LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.IsEnabled = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m llmProviderModelMods) RandomIsEnabled(f *faker.Faker) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.IsEnabled = func() bool {
			return random_bool(f)
		}
	})
}

// Set the model columns to this value
func (m llmProviderModelMods) DiscoveredAt(val time.Time) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.DiscoveredAt = func() time.Time { return val }
	})
}

// Set the Column from the function
func (m llmProviderModelMods) DiscoveredAtFunc(f func() time.Time) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.DiscoveredAt = f
	})
}

// Clear any values for the column
func (m llmProviderModelMods) UnsetDiscoveredAt() LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.DiscoveredAt = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m llmProviderModelMods) RandomDiscoveredAt(f *faker.Faker) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(_ context.Context, o *LLMProviderModelTemplate) {
		o.DiscoveredAt = func() time.Time {
			return random_time_Time(f)
		}
	})
}

func (m llmProviderModelMods) WithParentsCascading() LLMProviderModelMod {
	return LLMProviderModelModFunc(func(ctx context.Context, o *LLMProviderModelTemplate) {
		if isDone, _ := llmProviderModelWithParentsCascadingCtx.Value(ctx); isDone {
			return
		}
		ctx = llmProviderModelWithParentsCascadingCtx.WithValue(ctx, true)
		{

			related := o.f.NewLLMProviderConfigWithContext(ctx, LLMProviderConfigMods.WithParentsCascading())
			m.WithProviderConfigLLMProviderConfig(related).Apply(ctx, o)
		}
	})
}

func (m llmProviderModelMods) WithProviderConfigLLMProviderConfig(rel *LLMProviderConfigTemplate) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(ctx context.Context, o *LLMProviderModelTemplate) {
		o.r.ProviderConfigLLMProviderConfig = &llmProviderModelRProviderConfigLLMProviderConfigR{
			o: rel,
		}
	})
}

func (m llmProviderModelMods) WithNewProviderConfigLLMProviderConfig(mods ...LLMProviderConfigMod) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(ctx context.Context, o *LLMProviderModelTemplate) {
		related := o.f.NewLLMProviderConfigWithContext(ctx, mods...)

		m.WithProviderConfigLLMProviderConfig(related).Apply(ctx, o)
	})
}

func (m llmProviderModelMods) WithExistingProviderConfigLLMProviderConfig(em *models.LLMProviderConfig) LLMProviderModelMod {
	return LLMProviderModelModFunc(func(ctx context.Context, o *LLMProviderModelTemplate) {
		o.r.ProviderConfigLLMProviderConfig = &llmProviderModelRProviderConfigLLMProviderConfigR{
			o: o.f.fromExistingLLMProviderConfig(ctx, em),
		}
	})
}

func (m llmProviderModelMods) WithoutProviderConfigLLMProviderConfig() LLMProviderModelMod {
	return LLMProviderModelModFunc(func(ctx context.Context, o *LLMProviderModelTemplate) {
		o.r.ProviderConfigLLMProviderConfig = nil
	})
}
